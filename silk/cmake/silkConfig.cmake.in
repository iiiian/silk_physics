@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
find_dependency(Eigen3 REQUIRED)

include("${CMAKE_CURRENT_LIST_DIR}/silkTargets.cmake")

# Optionally propagate variables, e.g. version, compile defs, etc.
